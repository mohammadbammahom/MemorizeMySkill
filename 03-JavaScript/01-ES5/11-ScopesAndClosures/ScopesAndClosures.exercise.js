/*
تمرین شخصی

در این فایل باید موارد زیر را خودت پیاده‌سازی کنی.

1. یک تابع بساز که یک متغیر محلی داشته باشد.
2. یک تابع داخلی درون آن تعریف کن.
3. از تابع داخلی برای بازگرداندن یک مقدار استفاده کن.
4. یک مثال ساده از بسته‌بودن داده با closure بساز.
5. خروجی‌ها را با console.log نمایش بده.

پس از پایان تمرین، فایل باید بدون خطا با دستور زیر اجرا شود:

node ScopesAndClosures.exercise.js
*/

// کد خودت را از اینجا بنویس.
