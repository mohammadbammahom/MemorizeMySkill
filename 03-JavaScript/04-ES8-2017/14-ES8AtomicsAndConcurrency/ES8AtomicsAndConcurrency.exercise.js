/*
تمرین شخصی

در این فایل باید موارد زیر را خودت پیاده‌سازی کنی.

1. یک SharedArrayBuffer و یک typed array روی آن بساز.
2. با Atomics.load، Atomics.add یا Atomics.store عملیات ساده‌ای انجام بده.
3. نتیجه را با console.log نمایش بده.
4. توضیح کوتاه درباره race condition و اهمیت Atomics بنویس.
بعد از انجام تمرین،
فایل باید بدون خطا اجرا شود:

node ES8AtomicsAndConcurrency.exercise.js
*/

// کد خودت را از اینجا بنویس.
